<h1>Links</h1>

<% if User.last %>
<h2>Welcome <%=User.last.email%></h2>
<% end %>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL: <%= link.url %>
      Tag: <%= link.tags.map(&:name)%>
    </li>
  <% end %>
</ul>
